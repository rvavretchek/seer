# Testes

Testes do orquestrador e das skills. Formato automatizado ainda não definido — o que existe hoje é `manual/`, roteiros de teste manual pra validar o plugin dentro de superfícies reais (Cowork, Claude Code) que nenhum teste automatizado ainda cobre.

- [`manual/cowork-rc1-test-script.md`](manual/cowork-rc1-test-script.md) — roteiro pra validar a v1.0.0-rc.1 dentro do app Cowork real: invocação automática de skill, despacho do orquestrador, o conector OpenAlex, e acesso a pasta.

---

# Tests (English / en-US)

Orchestrator and skill tests. Automated format not yet defined — what exists today is `manual/`, manual test scripts to validate the plugin inside real surfaces (Cowork, Claude Code) that no automated test covers yet.

- [`manual/cowork-rc1-test-script.md`](manual/cowork-rc1-test-script.md) -- script to validate v1.0.0-rc.1 inside the real Cowork app: automatic skill invocation, orchestrator dispatch, the OpenAlex connector, and folder access.
