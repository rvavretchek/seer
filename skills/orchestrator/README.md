# Orquestrador

A meta-skill do Seer: decompõe a intenção do pesquisador em disciplinas, métodos e fontes necessárias, e compõe as skills de `skills/core/`, `skills/education/` e `skills/geography/` (e pacotes futuros) para executar. Ver Constituição, Princípio 4 — composição, não coleção.

**Status:** no ar — `orchestrator/SKILL.md`. Generaliza o padrão `research-coordinator` de `vendor/agent-papers-cli/` (tabela fixa de 3 workflows) para descoberta dinâmica: lê `domain`/`chains_well_with`/`description` de todo `skills/**/SKILL.md` em tempo real, em vez de uma tabela hardcoded — necessário porque o elenco de skills do Seer cresce por contribuição da comunidade.

---

# Orchestrator (English / en-US)

Seer's meta-skill: decomposes the researcher's intent into the disciplines, methods, and sources needed, and composes skills from `skills/core/`, `skills/education/`, and `skills/geography/` (and future packs) to execute. See Constitution, Principle 4 — composition, not collection.

**Status:** live — `orchestrator/SKILL.md`. Generalizes the `research-coordinator` pattern in `vendor/agent-papers-cli/` (a fixed 3-workflow table) into runtime discovery: reads `domain`/`chains_well_with`/`description` from every `skills/**/SKILL.md` live, instead of a hardcoded table -- necessary because Seer's skill roster grows by community contribution.
